<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zimax.mcrs.device.mapper.DeviceMapper">

    <!--查询记录数-->
    <select id="count" resultType="int">
        select count(*) from eqi_device
        <where>
            <if test="equipmentId !=null and equipmentId !=''">
                and equipment_id like '%${equipmentId}%'
            </if>
            <if test="APPId !=null and APPId !=''">
                and app_id like '%${APPId}%'
            </if>
        </where>
    </select>

    <!--查询所有-->
    <select id="queryAll" parameterType="map" resultType="com.zimax.mcrs.device.pojo.Device">
        select * from eqi_device
        <where>
            <if test="equipmentId !=null and equipmentId !=''">
                and equipment_id like '%${equipmentId}%'
            </if>
            <if test="APPId !=null and APPId !=''">
                and app_id like '%${APPId}%'
            </if>
        </where>
        <if test="order !=null and order !=''">
            order by ${field} ${order}
        </if>
        <if test="limit !=null and limit !=''">
            limit ${begin},${limit}
        </if>
    </select>

    <!--注册终端-->
    <insert id="registrationDevice" parameterType="com.zimax.mcrs.device.pojo.Device">
        insert into eqi_device
        values (#{APPId}, #{version}, #{needUpdate},
                #{deviceSoftwareType}, #{deviceName},
                #{assessName}, #{factoryName}, #{assessType},
                #{assessIp}, #{equipmentId},
                #{assessAttributes}, #{assessInstallLocation},
                #{accessMethod}, #{remarks}, #{creator},
                #{createTime}, #{enable})
    </insert>

    <!--根据主键删除设备-->
    <delete id="logoutDevice" parameterType="String">
        delete
        from eqi_device
        where app_id = #{APPId}
    </delete>

    <!--更新1条设备信息-->
    <update id="updateDevice" parameterType="com.zimax.mcrs.device.pojo.Device">
        update eqi_device
        set version=#{version},
            need_update=#{needUpdate},
            device_software_type=#{deviceSoftwareType},
            device_name=#{deviceName},
            assess_name=#{assessName},
            factory_name=#{factoryName},
            assess_type=#{assessType},
            assess_ip=#{assessIp},
            equipment_id=#{equipmentId},
            assess_attributes=#{assessAttributes},
            assess_install_location=#{assessInstallLocation},
            access_method=#{accessMethod},
            remarks=#{remarks},
            enable=#{enable}
        where app_id = #{APPId}
    </update>


</mapper>
