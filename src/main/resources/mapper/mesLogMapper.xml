<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zimax.mcrs.log.mapper.MesLogMapper">

    <!--    查询记录数-->
    <select id="count" resultType="int">
        SELECT count(*) FROM log_mes_log AS lml
        LEFT JOIN eqi_equipment AS eqi
        ON lml.equipment_int = eqi.equipment_int
        LEFT JOIN eqi_device AS dev
        ON lml.device_id = dev.device_id
        LEFT JOIN base_equip_info AS bei
        ON eqi.equip_type_id = bei.equip_type_id
        LEFT JOIN cap_user AS cur
        ON lml.creator = cur.USER_ID
        <where>
            <if test="equipmentId !=null and equipmentId !=''">
                and eqi.equipment_id like '%${equipmentId}%'
            </if>
            <if test="deviceName!=null and deviceName !=''">
                and dev.device_name like '%${deviceName}%'
            </if>
            <if test="mesIpAddress !=null and mesIpAddress !=''">
                and bei.mes_ip_address like '%${mesIpAddress}%'
            </if>
            <if test="equipmentContinuePort !=null and equipmentContinuePort !=''">
                and eqi.equipment_continue_port like '%${equipmentContinuePort}%'
            </if>
            <if test="createTime !=null and createTime !=''">
                and lml.create_time like '%${createTime}%'
            </if>
        </where>
    </select>

    <!--查询所有-->
    <select id="queryAll" parameterType="map" resultType="com.zimax.mcrs.log.pojo.MesLogVo">
        SELECT *,cur.USER_NAME AS createName FROM log_mes_log AS lml
        LEFT JOIN eqi_equipment AS eqi
        ON lml.equipment_int = eqi.equipment_int
        LEFT JOIN eqi_device AS dev
        ON lml.device_id = dev.device_id
        LEFT JOIN base_equip_info AS bei
        ON eqi.equip_type_id = bei.equip_type_id
        LEFT JOIN cap_user AS cur
        ON lml.creator = cur.USER_ID
        <where>
            <if test="equipmentId !=null and equipmentId !=''">
                and eqi.equipment_id like '%${equipmentId}%'
            </if>
            <if test="deviceName!=null and deviceName !=''">
                and dev.device_name like '%${deviceName}%'
            </if>
            <if test="mesIpAddress !=null and mesIpAddress !=''">
                and bei.mes_ip_address like '%${mesIpAddress}%'
            </if>
            <if test="equipmentContinuePort !=null and equipmentContinuePort !=''">
                and eqi.equipment_continue_port like '%${equipmentContinuePort}%'
            </if>
            <if test="createTime !=null and createTime !=''">
                and lml.create_time like '%${createTime}%'
            </if>
        </where>
        <if test="order !=null and order !=''">
            order by ${field} ${order}
        </if>
        <if test="limit !=null and limit !=''">
            limit ${begin},${limit}
        </if>
    </select>

    <!--添加Rfid交换日志-->
    <insert id="addMesLog" parameterType="com.zimax.mcrs.log.pojo.MesLog">
        insert into log_mes_log
        values (#{mesLogId}, #{mesLogNum}, #{logType}, #{equipmentInt},
                #{deviceId}, #{content}, #{creator},
                #{createTime})
    </insert>

    <!--删除-->
    <delete id="removeMesLog" parameterType="int">
        delete from log_mes_log where mes_log_id = #{mesLogId}
    </delete>


</mapper>
